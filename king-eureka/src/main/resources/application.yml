server:
  port: 10086
spring:
  application:
    name: king-erueka
eureka:
  client:
    #是否检索服务
    fetch-registry: false
    #是否向服务注册中心注册自己
    register-with-eureka: false
    service-url:
      defaultZone: http://127.0.0.1:${server.port}/eureka