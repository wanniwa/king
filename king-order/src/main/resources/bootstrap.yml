spring:
  application:
    name: king-order
  cloud:
    config:
      discovery:
        enabled: true
        service-id: king-config
      profile: dev
  rabbitmq:
    host: 106.13.105.158
    port: 5672
    username: guest
    password: wang1993
eureka:
  client:
    service-url:
      # 指定eureka server通信地址，注意/eureka/小尾巴不能少
      defaultZone: http://localhost:8761/eureka
